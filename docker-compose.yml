services:
  db:
    image: postgres:16
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: celine_hair
    ports:
      - "5433:5432"
    volumes:
      - celine_hair_db:/var/lib/postgresql/data

volumes:
  celine_hair_db:
